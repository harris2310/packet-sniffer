#include <iostream>
#include <winsock2.h>
#include <string>
#include <ws2tcpip.h>
#include <vector>
#include "ApiKey.h"

int main()
{
  ApiKey apiKey;
  SOCKET s = socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
  std::vector<int> a;
  return 0;
}
